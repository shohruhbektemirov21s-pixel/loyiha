# Runtime deps for the serving layer. Pin exactly — in an air-gapped deploy
# these wheels are mirrored to a local index; floating versions break repro.
fastapi==0.135.3
uvicorn[standard]==0.38.0
pydantic==2.12.5
pydantic-settings==2.14.1
# httpx is pulled in by fastapi.testclient (dev/test). Listed for the mirror.
httpx==0.28.1
# datalayer: AES-256-GCM at-rest encryption (AesGcmEncryptor in storage.py)
cryptography==45.0.3
# datalayer: balance analysis and augmentation weight computation
numpy==2.2.6
# datalayer: DVC-compatible YAML manifest stubs in versioning.py
PyYAML==6.0.2
# -- Backend: PostgreSQL persistence (app/db/) ---------------------------
sqlalchemy[asyncio]==2.0.41
asyncpg==0.30.0          # async PostgreSQL driver used by SQLAlchemy asyncio
# -- Authentication (app/auth/) -----------------------------------------
python-jose[cryptography]==3.3.0   # JWT encode/decode (HS256)
bcrypt==4.3.0                      # password hashing (used directly, no passlib wrapper)
# -- WebSocket support (uvicorn[standard] already pulls websockets) ------
websockets==15.0.1
# -- Observability (app/metrics.py: Prometheus /metrics endpoint) --------
# Scraped by deploy/prometheus/prometheus.yml. Without it the API still boots
# (instrumentation is wired best-effort) but exposes no /metrics.
prometheus-client==0.21.1
# -- Acquisition layer (acquisition/) -----------------------------------
pydicom==3.0.1                     # DICOS/DICOM file parsing (dicos.py)
opencv-python-headless==4.11.0.86  # HDMI frame grabber capture (framegrab.py)
Pillow==11.2.1                     # TIFF encoding for raw pixel buffers
